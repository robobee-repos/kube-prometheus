apiVersion: v1
kind: Service
metadata:
  labels:
    name: prometheus-adapter
    addonmanager.kubernetes.io/mode: Reconcile
  name: prometheus-adapter
  namespace: monitoring
spec:
  ports:
  - name: https
    port: 443
    targetPort: 6443
  selector:
    name: prometheus-adapter
